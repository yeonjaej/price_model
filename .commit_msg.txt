docs(readme): tone pass — academic, professional, concise

Convert first-person constructions to passive voice or third-person
throughout. No "we", "our", "I", or "you" addressing the reader. All
numbers, claims, code references, and the ablation matrix preserved.

Section-level changes:

- "Executive Summary" → "Executive summary" (sentence-case heading
  convention) and the bullets cleaned up to consistent academic phrasing.
- "How the headline was earned — three orthogonal ablations" →
  "Headline result: ablation analysis". Body rewritten to passive /
  third-person; intent unchanged.
- "Subset universe, PIT ablation (22-feature model)" →
  "Universe × PIT × regime ablation (22-feature model)" (more accurate
  description of the matrix axes).
- "Three independent readings fall out of this" → "Three observations
  follow". Bullets reworded with measured academic register
  ("A practitioner running ... would observe ... and infer a credible
  edge ...") rather than rhetorical asides.
- "Reproduce the ablations" → "Reproduction". Stage commands trimmed
  and the orphaned HTML comment about the chronological build was
  dropped (the same content now lives elsewhere in less obtrusive form).
- "Data quality and methodological limitations": meta-sentences
  removed ("This is the section I'd want any reviewer to read"). The
  yfinance limitations enumeration retained verbatim aside from
  rephrasing ownership ("our drop list" → "the project drop list").
- "Upward bias due to data limitations" → "Sources of upward bias in
  the headline". All five caveats rephrased in declarative academic
  voice. The single-window caveat now states "A multi-window
  robustness check has not been performed" rather than "I didn't run".
- "What would need to change to deliver a *true* PIT-correct number"
  → "Toward a fully PIT-correct evaluation".
- "Honest scope — what this is NOT" → "Scope and limitations".
- "On the Streamlit dashboard" → "Streamlit dashboard".
- "Development notes": Claude Code attribution preserved. Light prose
  cleanup; "Treat this README ... as the author's claims" remains as
  the closing line but in third-person form.
- Headings normalized to sentence-case.

Final state: no instances of we / our / I / you remain in the README
(other than the country code "US"). No source code changes; 91 tests
passing on the local-runnable subset (test_store has a pre-existing
unrelated collection error on the sandbox-only Python 3.10).
